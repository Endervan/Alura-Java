package modelo.basico;

// interface marcadora para as entidades do sistema apenas msm classes seja persistidas
// ou não, ou seja, não é necessário que as classes implementem métodos específicos
// para serem consideradas entidades, apenas precisam ser marcadas como tal.
// Isso é útil para fins de organização e identificação de classes que representam
// entidades no sistema, facilitando a manutenção e compreensão do código.
public interface Entidade {
}
